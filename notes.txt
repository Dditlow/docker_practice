docker run --name shrek -p 8080:80 -v /Users/danielditlow/Documents/repositories/docker_practice/httpsource:/usr/share/nginx/html:ro -d nginx
docker run --name dragon -e PGHOST=localhost -e PGUSER=postgres -e POSTGRES_PASSWORD=postgres -e PGDATABASE=postgres -d -p 5432:5432 postgres
PGPASSWORD=postgres psql -U postgres -h localhost -d postgres



CREATE TABLE app_users (
     id   integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
     first_name   varchar(40) NOT NULL,
     last_name   varchar(45) NOT NULL,
     usr_name   varchar(40),
     age   smallserial
);

POSTGRES table data
INSERT INTO app_users(first_name, last_name, usr_name, age)
VALUES ('Francis', 'Link', 'patmystomach', 36);

INSERT INTO app_users(first_name, last_name, usr_name, age)
VALUES ('Benni', 'Hanna', 'onionvolcano', 17);

INSERT INTO app_users(first_name, last_name, usr_name, age)
VALUES ('Don', 'Key', 'deadmutten', 14);

INSERT INTO app_users(first_name, last_name, usr_name, age)
VALUES ('Hey', 'Zeus', 'thecross', 1986);

INSERT INTO app_users(first_name, last_name, usr_name, age)
VALUES ('Ringo', 'Star', 'drumboy', 77);